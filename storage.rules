rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    /*
     * WatchTogether 目前前端沒有使用 Firebase Storage。
     * 暫時全部拒絕，避免匿名/一般登入者任意讀寫房間媒體。
     */
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
