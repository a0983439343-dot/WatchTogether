name: Stable playback authority repair

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  repair:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install room authority
        run: python3 .github/scripts/install_playback_v12.py

      - name: Harden YouTube callbacks
        run: python3 .github/scripts/harden_playback_v13.py

      - name: Validate JavaScript
        run: |
          node --check app.js
          grep -q 'ROOM AUTHORITY PLAYBACK V12' app.js
          grep -q 'state.roomAuthorityV12' app.js
          grep -q 'roomAuthorityInstallControls' app.js
          grep -q 'roomAuthorityStartObserver' app.js
          grep -q 'roomAuthorityExpectedPosition' app.js

      - name: Commit
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add app.js
          if git diff --cached --quiet; then
            echo 'No changes'
          else
            git commit -m 'Finalize authoritative room playback synchronization'
            git push origin HEAD:main
          fi
