name: Record Validate WatchTogether Status

on:
  workflow_run:
    workflows:
      - Validate WatchTogether
    types:
      - completed

permissions:
  contents: write

jobs:
  record:
    if: github.event.workflow_run.event == 'push' && github.event.workflow_run.head_branch == 'main'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Record validation result
        env:
          RUN_ID: ${{ github.event.workflow_run.id }}
          RUN_NUMBER: ${{ github.event.workflow_run.run_number }}
          CONCLUSION: ${{ github.event.workflow_run.conclusion }}
          HEAD_SHA: ${{ github.event.workflow_run.head_sha }}
          HTML_URL: ${{ github.event.workflow_run.html_url }}
          UPDATED_AT: ${{ github.event.workflow_run.updated_at }}
        run: |
          mkdir -p ci-status
          node - <<'NODE'
          const fs = require("fs");
          const data = {
            workflow: "Validate WatchTogether",
            runId: Number(process.env.RUN_ID),
            runNumber: Number(process.env.RUN_NUMBER),
            conclusion: process.env.CONCLUSION,
            headSha: process.env.HEAD_SHA,
            url: process.env.HTML_URL,
            updatedAt: process.env.UPDATED_AT,
            rulesDeployed: process.env.CONCLUSION === "success"
          };
          fs.writeFileSync("ci-status/validate-latest.json", JSON.stringify(data, null, 2) + "\n");
          NODE

          git config user.name "WatchTogether CI"
          git config user.email "watchtogether-ci@users.noreply.github.com"
          git add ci-status/validate-latest.json
          git diff --cached --quiet && exit 0
          git commit -m "ci: record validation status"
          git push origin HEAD:main
